# app/

Next.js (App Router, TypeScript). Route Handlers para la API (`/api/v1/posts/...`), UI mínima de demostración, y el mock del proveedor externo.

Pendiente: `npx create-next-app@latest` con TypeScript + App Router.
